# ============================================================
# Render 啟動設定
# ============================================================
# Render 會自動讀取這個檔案來啟動伺服器。
# $PORT 是 Render 自動設定的埠口（通常為 10000）
# ============================================================
web: uvicorn main:app --host 0.0.0.0 --port $PORT
